<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <%= link_to "Smart Banking", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to t('global.menu.about_us'), '/about', class: 'nav-link' %>
                </li>
                <li class="nav-item">
                    <%= link_to t('global.menu.support'), '/support', class: 'nav-link' %>
                </li>
            </ul>

            <ul class="navbar-nav ml-auto">
                <% if current_user %>
                    <li class="nav-item my-auto">
                    <% if current_user.avatar.attached? %>
                        <a href="<%=users_url%>"><img src="<%= (url_for(current_user.avatar)) %>" alt="Admin" class="rounded-circle " width="40" height="40" ></a>
                    <% else %>
                        <a href="<%=users_url%>"> <img src="https://smart-banking.s3.us-east-2.amazonaws.com/iconfinder_2428662_account_agenda_app_contacts_online_icon_512px+(1).png" alt="Admin" class="rounded-circle" width="40"></a>
                     <% end %>
                    </li>
                  <li class="nav-item">
                    <%= link_to t('global.menu.user_profile'), users_url, class: 'nav-link' %>
                  </li>
                <% end %>
                <% if current_user %> 
                    <li class="nav-item mr-5">
                        <%= link_to t('global.menu.sign_out'), destroy_user_session_path, class: "nav-link", method: :delete %>
                    </li>
                <% else %>
                    <li class="nav-item mr-5">
                        <%= link_to t('global.menu.sign_in'), new_user_session_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item mr-5">
                        <%= link_to  t('global.menu.sign_up'), new_user_registration_path, class: "nav-link" %>
                    </li>
                <% end %> 
                <li class="nav-item my-auto">
                    <% if locale == :ru %>
                        <%= link_to 'ENG', url_for(locale: :en), class: "btn btn-outline-secondary btn-sm" %>
                    <% else %>
                        <%= link_to 'RU', url_for(locale: :ru), class: "btn btn-outline-secondary btn-sm" %>
                    <% end %>
                    <button id="changeTheme">Dark text</button>
                </li>
            </ul>
        </div>
    </div>
</nav>
